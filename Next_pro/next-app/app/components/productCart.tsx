'use client'

import React from 'react'
const productCart = () => {
  return (
    <div>
    <button  onClick={()=>console.log("the button is clicked")}>Add</button>
    </div>
  )
}

export default productCart
